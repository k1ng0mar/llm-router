# runtime state (never commit)
*.db
*.db-shm
*.db-wal
*.log
.env
*.bak*
llm-router      # built binary
quota.json      # runtime quota state
router.yaml     # real config with secrets
router_8016.yaml

# local-only
.vscode/
.idea/
